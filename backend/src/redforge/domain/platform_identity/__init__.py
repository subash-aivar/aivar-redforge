"""Platform Identity bounded context.

Models the PLATFORM control plane — authorization that spans all
organizations — as a concept entirely separate from organization
(tenant) membership and roles. See `docs/M1_PLATFORM_IDENTITY_SUPER_ADMIN_REPORT.md`
for the full architecture rationale.
"""
