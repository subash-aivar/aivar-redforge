"""Core module — cross-cutting concerns, configuration, and domain exception hierarchy.

This is the center ring of the architecture. Nothing in this package
depends on outer rings (infrastructure, api). Everything else may depend on core.
"""
