"""ORM ↔ Domain entity mapping layer.

Mappers translate between the persistence representation (ORM models)
and the domain representation (domain entities/value objects). This
isolates the domain from ORM details and keeps the mapping logic
centralized and testable.
"""
