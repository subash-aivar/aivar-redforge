"""Provider adapter implementations.

Each AI provider has its own adapter that implements the ProviderAdapter
Protocol from the execution engine domain. Adapters handle HTTP communication,
token usage tracking, error normalization, and retry logic.
"""
