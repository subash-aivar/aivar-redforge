"""M29 Operator bounded context — red team operator identity and clearance.

Python package is ``red_team_operator`` because ``operator`` shadows the stdlib.
Database schema remains ``operator``.
"""
